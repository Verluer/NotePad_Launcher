<Window x:Class="NotePad_Launcher.EncryptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:commands="clr-namespace:NotePad_Launcher.MVVM.Commands"
        mc:Ignorable="d"
        Title="EncryptionWindow" Height="400" Width="600"
        MinWidth="600" MinHeight="400" MaxWidth="600" MaxHeight="400"
        Style="{StaticResource WindowStyle}">
    <Window.Resources>
        <commands:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="50" />
            <RowDefinition Height="80" />
            <RowDefinition Height="80"/>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!--Заголовок программы (Именование и три кнопки функционала)-->
        <DockPanel Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" Background="{StaticResource HeadColor}" LastChildFill="false" Height="30"  MouseDown="HeadLine_MouseDown">
            <TextBlock Text=" En/decryption" DockPanel.Dock="Left" FontSize="16" Foreground="WhiteSmoke" Height="26"/>
            <Button DockPanel.Dock="Right" Command="{Binding CloseCommand}" Content="✖">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource ButtonHeadStyle}">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Red"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button DockPanel.Dock="Right" Command="{Binding MinimizeCommand}" Content="━" Style="{StaticResource ButtonHeadStyle}"/>
        </DockPanel>
        <TextBlock Text="{Binding MethodName}" Grid.Column="1" Grid.Row="1" FontSize="18" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
        <StackPanel Grid.Column="0" Grid.Row="2" >
            <TextBlock  Text="{Binding TextBlockValue1}" FontSize="16" TextWrapping="Wrap" HorizontalAlignment="Center">
                <TextBlock.Visibility>
                    <Binding Path="IsElement1Visible" Converter="{StaticResource BoolToVisibilityConverter}" />
                </TextBlock.Visibility>
            </TextBlock>
            <TextBox Text="{Binding TextBoxValue1}"  Width="100" Height="30"  FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                <TextBox.Visibility>
                    <Binding Path="IsElement1Visible"  Converter="{StaticResource BoolToVisibilityConverter}" />
                </TextBox.Visibility>
            </TextBox>
        </StackPanel>

        <StackPanel Grid.Column="0" Grid.Row="3" >
            <TextBlock  Text="{Binding TextBlockValue2}" FontSize="16" TextWrapping="Wrap" HorizontalAlignment="Center">
                <TextBlock.Visibility>
                    <Binding Path="IsElement2Visible" Converter="{StaticResource BoolToVisibilityConverter}" />
                </TextBlock.Visibility>
            </TextBlock>
            <TextBox Text="{Binding TextBoxValue2}" Width="100" Height="30" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                <TextBox.Visibility>
                    <Binding Path="IsElement2Visible"  Converter="{StaticResource BoolToVisibilityConverter}" />
                </TextBox.Visibility>
            </TextBox>
        </StackPanel>
        <StackPanel Grid.Column="0" Grid.Row="4" >
            <TextBlock  Text="{Binding TextBlockValue3}"  FontSize="16" TextWrapping="Wrap" HorizontalAlignment="Center">
                <TextBlock.Visibility>
                    <Binding Path="IsTextBlock3Visible" Converter="{StaticResource BoolToVisibilityConverter}" />
                </TextBlock.Visibility>
            </TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBox Text="{Binding TextBoxValue3}"  Width="50" Height="30" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="5">
                    <TextBox.Visibility>
                        <Binding Path="IsTextBoxValue3Visible"  Converter="{StaticResource BoolToVisibilityConverter}" />
                    </TextBox.Visibility>
                </TextBox>
                <TextBox Text="{Binding TextBoxValue4}" x:Name="TextValue4" Width="50" Height="30" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="5">
                    <TextBox.Visibility>
                        <Binding Path="IsTextBoxValue4Visible"  Converter="{StaticResource BoolToVisibilityConverter}" />
                    </TextBox.Visibility>
                </TextBox>
                <TextBox Text="{Binding TextBoxValue5}" x:Name="TextValue5" Width="50" Height="30" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="5">
                    <TextBox.Visibility>
                        <Binding Path="IsTextBoxValue5Visible"  Converter="{StaticResource BoolToVisibilityConverter}" />
                    </TextBox.Visibility>
                </TextBox>
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Column="2" Grid.Row="3" >
            <TextBlock  Text="{Binding TextBlockCloseKey1}" FontSize="16" TextWrapping="Wrap" HorizontalAlignment="Center">
                <TextBlock.Visibility>
                    <Binding Path="IsTextBlockCloseKeyVisible" Converter="{StaticResource BoolToVisibilityConverter}" />
                </TextBlock.Visibility>
            </TextBlock>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBox Text="{Binding TextBoxCloseKey1}" x:Name="CloseKey1" Width="50" Height="30" FontSize="20" VerticalAlignment="Center" Margin="5">
                    <TextBox.Visibility>
                        <Binding Path="IsTextBoxCloseKey1Visible"  Converter="{StaticResource BoolToVisibilityConverter}" />
                    </TextBox.Visibility>
                </TextBox>
                <TextBox Text="{Binding TextBoxCloseKey2}" x:Name="CloseKey2" Width="50" Height="30" FontSize="20" VerticalAlignment="Center" Margin="5">
                    <TextBox.Visibility>
                        <Binding Path="IsTextBoxCloseKey2Visible"  Converter="{StaticResource BoolToVisibilityConverter}" />
                    </TextBox.Visibility>
                </TextBox>
                <TextBox Text="{Binding TextBoxCloseKey3}" x:Name="CloseKey3" Width="50" Height="30" FontSize="20" VerticalAlignment="Center" Margin="5">
                    <TextBox.Visibility>
                        <Binding Path="IsTextBoxCloseKey3Visible"  Converter="{StaticResource BoolToVisibilityConverter}" />
                    </TextBox.Visibility>
                </TextBox>
            </StackPanel>
        </StackPanel>
        <TextBlock  Grid.Column="2" Grid.Row="4" Text="{Binding TextBlockCloseKey}" FontSize="16" TextWrapping="Wrap" HorizontalAlignment="Center"/>
        <TextBlock  Grid.Column="2" Grid.Row="4" Text="{Binding TextBlockOpenKey}" FontSize="16" TextWrapping="Wrap" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
        <Button x:Name="EncryptionButton" Command="{Binding EncryptionCommand}"  Grid.Row="5" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="150" Height="50" FontSize="20">
            Encryption
        </Button>
        <Button Command="{Binding DigitalSignatureCommand}" Grid.Row="5" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="150" Height="50" FontSize="20">
            Digital signature
            <Button.Visibility>
                <Binding Path="IsButtonDigitalVisible" Converter="{StaticResource BoolToVisibilityConverter}" />
            </Button.Visibility>
        </Button>
        <Button Command="{Binding DecryptionCommand}" Grid.Row="5" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center" Width="150" Height="50" FontSize="20">
            Decryption
        </Button>
        <ComboBox Grid.Column="1" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center"  Width="100" Height="30" FontSize="20" ItemsSource="{Binding Mode}" SelectedItem="{Binding SelectedMode, Mode=TwoWay}">
            
        </ComboBox>
    </Grid>
</Window>
