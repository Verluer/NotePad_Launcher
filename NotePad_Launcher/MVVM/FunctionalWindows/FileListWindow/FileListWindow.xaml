<Window x:Class="NotePad_Launcher.FileListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:commands="clr-namespace:NotePad_Launcher.MVVM.Commands"
        xmlns:local="clr-namespace:NotePad_Launcher"
        mc:Ignorable="d"
        x:Name="FileList"
        Title="FileListWindow" Height="350" Width="250"
        MinWidth="250" MinHeight="350" MaxWidth="250" MaxHeight="350"
        Style="{StaticResource WindowStyle}">
    <Window.Resources>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="25"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="15"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="25"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="3" Background="{StaticResource HeadColor}" LastChildFill="false" Height="30"  MouseDown="HeadLine_MouseDown">
            <Image DockPanel.Dock="Left"  HorizontalAlignment="Center" VerticalAlignment="Center" Height="25" Source="pack://application:,,,/NotePad_Launcher;component/Resources/app.ico"/>
            <TextBlock Text=" FileList" DockPanel.Dock="Left" FontSize="16" Foreground="WhiteSmoke" Height="26"/>
            <Button DockPanel.Dock="Right" Command="{Binding CloseCommand}" Content="✖">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource ButtonHeadStyle}">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Red"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button DockPanel.Dock="Right" Command="{Binding MinimizeCommand}" Content="━" Style="{StaticResource ButtonHeadStyle}"/>
        </DockPanel>



        <Border Grid.Row="1" Grid.Column="1" Background="#F0F0F0" BorderThickness="1,2,1,1" BorderBrush="Gray">
            <ComboBox Grid.ColumnSpan="2" Grid.Row="3" HorizontalContentAlignment="Center" ItemsSource="{Binding FolderFileString}" SelectedItem="{Binding SelectedFolder}"  SelectedIndex="{Binding SelectedIndex}"/>
        </Border>
        <Border  Grid.Row="1" BorderThickness="0,2,0,0" BorderBrush="Gray">
            <Button  Command="{Binding BackFolderCommand}" Content="⇐" VerticalContentAlignment="Top" FontSize="15" BorderThickness="0"  Background="#F0F0F0"  />
        </Border>
        <Border  Grid.Row="1" Grid.Column="3" BorderThickness="0,2,0,0" BorderBrush="Gray">
            <Button  Command="{Binding NextFolderCommand}" Content="⇒" VerticalContentAlignment="Top" FontSize="15" BorderThickness="0" Background="#F0F0F0"/>
        </Border>
        <Border Grid.Row="2" Grid.Column="1" Background="#F0F0F0" >
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button  Command="{Binding FolderCommand}" Content="📁" BorderThickness="1"  BorderBrush="Gray" Background="#F0F0F0" MinWidth="25"/>
                <Button  Command="{Binding NewFolderCreateCommand}" Content="📁+" BorderThickness="1"  BorderBrush="Gray" Background="#F0F0F0" MinWidth="25"/>
                <Button  Command="{Binding NewFileCreateCommand}" Content="📝" BorderThickness="1"  BorderBrush="Gray" Background="#F0F0F0" MinWidth="25"/>
                <Button  Command="{Binding FolderEditNameCommand}" Content="🖉" BorderThickness="1"  BorderBrush="Gray" Background="#F0F0F0" MinWidth="25"/>
                <Button  Command="{Binding FolderDeleteCommand}" Content="🗑️" BorderThickness="1"  BorderBrush="Gray" Background="#F0F0F0" MinWidth="25"/>
                <Button  Command="{Binding UpdateCommand}" Content="↺" BorderThickness="1" FontSize="15" BorderBrush="Gray" Background="#F0F0F0" MinWidth="25" MaxWidth="25"/>
            </StackPanel>
        </Border>
        <ListView x:Name="FileListView" Grid.Row="3" Grid.Column="1" ItemsSource="{Binding FileListItem}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="pack://application:,,,/NotePad_Launcher;component/Resources/app.ico"
                         Width="16" Height="16" 
                         Margin="0,0,5,0" 
                         VerticalAlignment="Center" />
                        <TextBlock Text="{Binding FileName}" Foreground="Black">
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ListView}}" />
                    <Setter Property="Cursor" Value="Hand" />
                    <Setter Property="ContextMenu">
                        <Setter.Value>
                            <ContextMenu>
                                <MenuItem Header="Переименовать"
                                  Command="{Binding PlacementTarget.Tag.MenuRenameFileCommand,
                                                    RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                  CommandParameter="{Binding}" />
                                <MenuItem Header="Удалить"
                                  Command="{Binding PlacementTarget.Tag.MenuDeleteFileCommand,
                                                    RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                  CommandParameter="{Binding}" />
                                <MenuItem Header="Переместить"
                                  Command="{Binding PlacementTarget.Tag.MenuMoveFileCommand,
                                                    RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                  CommandParameter="{Binding}" />
                            </ContextMenu>
                        </Setter.Value>
                    </Setter>
                    <EventSetter Event="MouseLeftButtonUp" Handler="ListViewItem_MouseLeftButtonUp" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Создать" Command="{Binding NewFileCreateCommand}"/>
                    <MenuItem Header="Обновить" Command="{Binding UpdateCommand}"/>
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>
        <!--Canvas панели (две боковые и одна нижняя) для дизайна программы-->
        <Canvas Grid.Row="2" Grid.Column="0" Grid.RowSpan="2" Background="#F0F0F0" />
        <Canvas Grid.Row="2" Grid.Column="2" Grid.RowSpan="2" Background="#F0F0F0" />
        <Canvas Grid.Column="0" Grid.Row="4"  Grid.ColumnSpan="3" Background="Gray" />
        <!--#######################################################################################-->
    </Grid>
</Window>
